Scripts
=======

Rust Build
    Run the compiler               : cargo run <args>
    Build and test                 : cargo test

Rust Doc
    Generate public documents      : cargo doc --no-deps --open
    Generate development documents : cargo doc --no-deps --document-private-items --open
